spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/family_cloud
    username: ${POSTGRES_USER}
    password: ${POSTGRES_PASSWORD}

  jpa:
    hibernate:
      ddl-auto: update   # dev only
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080
